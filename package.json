{
  "name": "eduhive-fake-api",
  "version": "1.0.0",
  "description": "Fake API for EduHive",
  "main": "db.json",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
